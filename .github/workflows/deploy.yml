name: Build & Deploy Flutter Web

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout Flutter source
      - name: Checkout source
        uses: actions/checkout@v4

      # 2️⃣ Setup Flutter
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.38.7"
          channel: stable

      # 3️⃣ Install deps
      - name: Install dependencies
        run: flutter pub get

      # 4️⃣ BUILD (THIS IS THE IMPORTANT PART)
      - name: Build Flutter Web
        run: |
          flutter build web --release \
            --dart-define=API_URL=https://pmgjsza276.execute-api.us-east-1.amazonaws.com/prod

      # 5️⃣ Checkout GitHub Pages repo
      - name: Checkout Pages repo
        uses: actions/checkout@v4
        with:
          repository: mohamedarmas/mohamedarmas.github.io
          token: ${{ secrets.GH_PAGES_TOKEN }}
          path: pages

      # 6️⃣ Remove old files
      - name: Clean old build
        run: |
          rm -rf pages/*
          touch pages/.nojekyll

      # 7️⃣ Copy new build
      - name: Copy new build
        run: |
          cp -r build/web/* pages/

      # 8️⃣ Commit & push
      - name: Deploy
        run: |
          cd pages
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add .
          git commit -m "🚀 Deploy Flutter Web" || echo "No changes"
          git push
